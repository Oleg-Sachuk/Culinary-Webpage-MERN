const { Router } = require('express');
const router = Router();
const User = require('../models/user');

router.post('/getuser', async (req,res) => {
    try {
        
    } catch (error) {
        
    }
} )